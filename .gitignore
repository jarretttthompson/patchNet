node_modules/
dist/
errorScreenshots/
patchNet-Vault/
/cursors/
autosaves/
.claude/
.DS_Store
.playwright-mcp/
*.local
.env
.env.local
.env.*.local

# Private/local-only object plugins — see src/graph/localPlugins.ts.
# The folder itself is tracked (.gitkeep) so the eager glob always resolves;
# every other file inside stays local to the developer's machine.
src/graph/localObjects/*
!src/graph/localObjects/.gitkeep
scripts/.vault_rag.sqlite
src-tauri/target
