# patchNet Object Spec Standard — SUPERSEDED

> **Deprecated 2026-05-19.** This document was materially stale (wrong category
> count, non-existent `ObjectDef` base, wrong `MessageDef.args` type, no mention
> of `derivePorts`/`ensureArgs`/`platforms`/`keywords`).
>
> **The authoritative Object API contract is now → [`object-api.md`](./object-api.md).**
>
> For the step-by-step "how to add an object" procedure, see
> [`OBJECT_RECIPE.md`](./OBJECT_RECIPE.md).

This file is retained only so existing links/vault references don't 404. Do not
add content here — update `object-api.md` instead. A point-by-point list of what
this old doc got wrong is in the Appendix of `object-api.md`.
